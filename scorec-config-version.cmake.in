set(PACKAGE_VERSION "@SCOREC_MAJOR@.@SCOREC_MINOR@.@SCOREC_PATCH@")
if("@SCOREC_MAJOR@" EQUAL "${PACKAGE_FIND_VERSION_MAJOR}" AND
   ("@SCOREC_MINOR@" GREATER "${PACKAGE_FIND_VERSION_MINOR}" OR
    ("@SCOREC_MINOR@" EQUAL "${PACKAGE_FIND_VERSION_MINOR}" AND
     ("@SCOREC_PATCH@" GREATER "${PACKAGE_FIND_VERSION_PATCH}" OR
      "@SCOREC_PATCH@" EQUAL "${PACKAGE_FIND_VERSION_PATCH}"))))
  set(PACKAGE_VERSION_COMPATIBLE 1)
endif()
if("${PACKAGE_FIND_VERSION_MAJOR}" EQUAL "@SCOREC_MAJOR@" AND
   "${PACKAGE_FIND_VERSION_MINOR}" EQUAL "@SCOREC_MINOR@" AND
   "${PACKAGE_FIND_VERSION_PATCH}" EQUAL "@SCOREC_PATCH@")
  set(PACKAGE_VERSION_EXACT 1)
endif()
